// import VideoCompressor from 'video-compressor'
//
// export async function compress(videoFile, compressQuality) {
//   if (!videoFile) {
//     return videoFile;
//   }
//
//   const compressor = new VideoCompressor();
//   return await compressor.compress(videoFile, {
//     quality: compressQuality && compressQuality > 0 && compressQuality < 1 ? compressQuality : 0.6,  // 控制压缩质量，0.0 - 1.0
//   });
// }
